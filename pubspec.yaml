name: hideip_vpn
description: "hideip.net VPN client for Android: a no-account sing-box front end."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.1.1+7

environment:
  sdk: ^3.12.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Persist profiles + selected server across launches.
  shared_preferences: ^2.3.2
  # Keep local encryption keys in Android Keystore / iOS Keychain. Large
  # profile blobs stay in preferences only as authenticated ciphertext.
  flutter_secure_storage: 10.3.1
  # Public-IP lookup (proof the tunnel works).
  http: ^1.2.2
  # Verify signed public server catalogs before using any mirror response.
  cryptography: ^2.7.0
  # Parse Clash-family subscription bodies (YAML with a `proxies:` list).
  yaml: ^3.1.2
  # QR scanning via pure ZXing (FFI). No Google ML Kit / Play Services
  # dependency, so it works on GrapheneOS and other no-GMS devices too;
  # mobile_scanner's ML Kit backend returns null without Play Services.
  flutter_zxing: ^2.3.0
  # Receives hideip:// custom-scheme deep links (initial + stream). On iOS it
  # hooks the scene lifecycle automatically through FlutterSceneDelegate, so the
  # empty SceneDelegate this app already ships needs no manual forwarding.
  app_links: ^7.2.1
  in_app_purchase: ^3.3.0
  url_launcher: ^6.3.2
  # Whether the device has any network at all. The Connect button and the
  # "Connection failed" sheet both key off this: with no network there is
  # nothing to blame the tunnel for.
  connectivity_plus: ^6.1.0
  # System permission state (notifications, camera) as three answers rather
  # than two, so "never asked" and "denied" can be told apart.
  permission_handler: ^12.0.0
  # Local notifications: connection alerts and voting updates, two channels.
  flutter_local_notifications: ^19.0.0
  # The system file sheet behind "Open file" on the import screen, so a
  # WireGuard .conf or a sing-box .json can be handed over without pasting it.
  file_picker: ^12.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# App launcher icon: "hide" over the IP block on the dark panel. Sources are
# rendered by scripts/build-app-icons.sh (which also runs this generator).
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_background: "#0B0E14"   # same dark as the hero panel (logo doc 01-F)
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"
  adaptive_icon_monochrome: "assets/icon/app_icon_monochrome.png"   # Android 13 themed icon
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # hideip.net brand fonts (self-hosted, matches the website). See
  # ../hideip.net/brand-kit/typography.md: Geist = display/wordmark,
  # Inter = body, JetBrains Mono = numbers (IPs, ports, latencies).
  assets:
    - assets/world_geo.json

  fonts:
    - family: Geist
      fonts:
        - asset: assets/fonts/Geist-Variable.ttf
    - family: Onest
      fonts:
        - asset: assets/fonts/Onest-Variable.ttf
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Variable.ttf
    # One variable file: every weight the design asks for (500 to 800) is a
    # `wght` axis value. Static cuts silently ignored FontVariation and drew
    # all mono text at Regular.
    - family: JetBrainsMono
      fonts:
        - asset: assets/fonts/JetBrainsMono-Variable.ttf

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
